# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

inhibit_all_warnings!
use_frameworks!

workspace 'MLinViewSample.xcworkspace'

pod 'SnapKit'

target 'MLinViewSample' do
  project 'MLinViewSample.xcodeproj'

end
